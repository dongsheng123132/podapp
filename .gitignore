target/
node_modules/
dist/
.DS_Store
apps/*/src-tauri/target/
apps/*/src-tauri/gen/

# 官网是生成物（scripts/build-site.mjs 从 pods/*/podapp.json 生成）。
# 生成物不进仓库 —— 进了就会有人手改网页而不改清单，两边从此对不上。
website/

.vercel

# sidecar 是构建产物（scripts/build-sidecars.mjs 生成）。
# 进仓库就会和源码悄悄错开：改了 MCP 忘了重编，包里还是旧的而日志全绿。
apps/*/src-tauri/binaries/

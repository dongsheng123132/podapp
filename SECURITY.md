# 安全策略

## 报告漏洞

请使用 GitHub 仓库的 **Security > Report a vulnerability** 私密报告入口。
不要在公开 Issue 中附带利用代码、密钥、真实用户数据或未修复漏洞的完整细节。

报告应包含受影响版本、平台、最小复现、实际影响和建议修复。维护者确认后会在私密
Security Advisory 中协调修复与披露。

## 安全边界

- 皮肤是声明式 JSON，只允许短标记、固定颜色字段和圆角；不加载 URL，不执行 CSS/JS。
- Pod 默认无权限。文件、网络、AI 和宿主动作必须在清单中显式申请，并由运行时统一检查。
- GUI、无头调用和远端调用走同一权限闸，不能通过绕过按钮绕过授权。
- 客户端不应保存服务端密钥；日志、错误和命令输出不得泄露凭据。
- `.pod` 仍应视为第三方代码，只安装可信来源并在确认页检查权限。

安全修复只承诺进入最新发布分支；旧版用户应先升级后复现。

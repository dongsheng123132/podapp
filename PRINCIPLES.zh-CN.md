# AI GUI 实践原则

[English](./PRINCIPLES.md) · [AI GUI 宣言](./MANIFESTO.zh-CN.md) ·
[规范导航](./SPEC.md)

本文把《AI GUI 宣言》的价值观转化为可执行的产品与工程检查项。它是宣言的实践配套，不替代
PodApp Protocol。

## 先根据任务选择交互方式

增加 Chat 或 GUI 之前，先问三个问题：

1. **意图有多模糊？** 开放目标适合用语言表达，再由 AI 推理。
2. **行动后果有多大？** 不可逆、影响外部、产生费用或涉及敏感数据的行动，需要明确范围并
   确认。
3. **交互会重复多少次？** 高频重复的选择或处理流程，应该固化为稳定 Action 和可复用界面。

最后得到的可能是 Chat、Pod，或在两者之间切换的流程。目标不是不惜代价减少控件，而是使用
足以让行动被理解的最小交互。

## 十项实现原则

### 1. 给每个有业务意义的动作稳定身份

业务动作应拥有稳定 Action ID、带版本的输入输出 Schema、明确影响和失败状态。切换标签、
拖动窗口等界面行为不是业务动作。

**证据：** 不启动 GUI，也能检查完整动作契约。

### 2. 业务动作只实现一次

GUI、CLI、MCP、API、测试和 AI 调用都进入同一个无界面动作核心。界面只负责适配输入和展示
状态，不得重写业务行为。

**证据：** 修改动作时，不需要在多个界面同步修改同一逻辑。

### 3. 让拟执行的行动可以被检查

最终提交之前，展示对象、范围、重要输入、预期影响和关键假设。确认文案必须说清实际行动，
不能只问一句脱离上下文的“确定吗？”

**证据：** 只看确认界面，人就能解释接下来会发生什么。

### 4. 显示状态与不确定性

明确区分计划中、等待中、执行中、成功、失败、取消和冲突。区分用户明确提供的值与模型推测的
值，不能把猜测伪装成已验证事实。

**证据：** 当前状态与关键值的来源可以被机器读取。

### 5. 把权限闸放在所有界面之下

权限要窄范围声明，并由动作核心或宿主统一执行，不能依赖 GUI 按钮是否可见。绕过界面绝不能
绕过授权。

**证据：** 同一个未授权调用从 GUI、无头、CLI 和 MCP 进入时都会被拒绝。

### 6. 先设计恢复，再扩大自治

优先提供预览、草稿、版本、幂等、撤销和回滚。确实无法逆转的行动，要在执行前明确说明，并把
影响范围压到最小。

**证据：** 测试覆盖重试、部分失败、陈旧状态和恢复路径。

### 7. 让人的控制权始终可达

长任务支持取消或安全中断。人可以修改假设、拒绝提案、重试失败步骤、切换手动操作，并保留
已经完成的工作。

**证据：** 控制入口在执行过程中仍然存在，而不是只在开始之前存在。

### 8. 让 Pod 保持小而可组合

一个 Pod 只负责一个清楚动作或高度相关的一组动作。输入、输出、产物、事件和状态都使用结构化
契约，让其他 Pod 或 AI 无需截图识别就能连接。

**证据：** Pod 可以无头调用，其输出可以成为下一个 Action 的输入。

### 9. 把无障碍能力视为机器接口的一部分

控件拥有稳定的非视觉标识、名称、角色、值与键盘行为。这既帮助使用辅助技术的人，也给自动化
提供可靠的非像素路径。

**证据：** 通过无障碍树也能理解关键操作路径。

### 10. 在正确的层级验证行为

业务正确性直接测试动作核心；界面是否绑定同一动作检查契约；远程、重试和乱序通过录制信封
重放；人的可见性和可理解性才交给少量 UI 与无障碍测试。截图只能证明外观，不能证明业务正确。

**证据：** 每次失败都能定位到动作、绑定、传输或展示中的某一层。

## 发布前检查

发布 Pod 或 AI GUI 能力之前，确认：

- [ ] Action 拥有稳定 ID，以及输入、输出和影响 Schema。
- [ ] 人与机器调用走同一条实现路径。
- [ ] 关键推测值与不确定性可以被看见。
- [ ] 重要行动在提交前展示对象、范围与影响。
- [ ] 权限最小化、已声明，并在 UI 之下统一执行。
- [ ] 执行状态可观察，并能在安全时中断。
- [ ] 重试、陈旧状态、部分失败和恢复都有定义。
- [ ] 输出是结构化结果或产物引用，而不是藏在屏幕里。
- [ ] 重要控件拥有稳定无障碍标识。
- [ ] 测试直接验证 Action，并在干净环境验证最终交付物。

花钱、对外发送、修改共享状态、授予访问权限或不可撤销的动作，应采用更严格的检查标准。

---

宣言定义方向，实践原则定义设计纪律。具有规范效力的包格式与动作契约见
[SPEC.md](./SPEC.md)。
